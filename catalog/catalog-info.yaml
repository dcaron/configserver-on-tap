apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: configserver
  description: Spring Cloud Config Server
  tags:
    - app-accelerator
    - java
    - spring
    - web
    - tanzu
  annotations:
    'backstage.io/kubernetes-label-selector': 'app.kubernetes.io/part-of=configserver'
spec:
  type: service
  lifecycle: experimental
  owner: default-team
